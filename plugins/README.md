# Plugins

Add one JSON file per plugin:

```text
plugins/com.lfen.toolbag.hash.json
```

Each file should list all releases for that plugin. The Registry build picks the newest release for `index.json`.
